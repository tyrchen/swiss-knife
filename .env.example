# AWS Configuration for s3upload tool
# Copy this file to .env and fill in your values

# AWS Region (e.g., us-west-2, us-east-1, eu-west-1)
AWS_REGION=us-west-2

# AWS Profile (optional - uses default if not specified)
# AWS_PROFILE=default

# S3 Bucket name
S3_BUCKET=my-bucket-name

# S3 Target path prefix (optional - leave empty for root of bucket)
# Example: "uploads" or "uploads/videos"
S3_TARGET_PATH=

# Log Level (optional - defaults to "info")
# Options: error, warn, info, debug, trace
# Can also use RUST_LOG environment variable for more advanced filtering
# Examples:
#   LOG_LEVEL=debug          - Show debug logs
#   LOG_LEVEL=error          - Only show errors
#   LOG_LEVEL=trace          - Very verbose logging
# LOG_LEVEL=info
